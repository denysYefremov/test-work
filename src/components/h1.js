// @flow
import styled from 'styled-components';

const H1 = styled.h1`
  font-size: var(--font-size-large);
  line-height: var(--line-height-large);
  margin: 0;
  padding: 0;
  padding-bottom: 10px;
`;

export default H1;
